import { createContext } from 'react';

const playerContext = createContext();

export default playerContext;

// Before having contols component
// we need to click the playlist so that Which song is been playingg We must noww..
//Ccreate context floder(3 components in them)